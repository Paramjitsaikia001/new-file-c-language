#include<stdio.h>
#include<math.h>
int main()
{
    int n;
    printf("enter the max power");
    scanf("%d",&n);
      printf("the coefficient from left to right:")
    for(int i=n;i>0;i++)
    {
      
    }
    return 1;
    
    return 1;
}